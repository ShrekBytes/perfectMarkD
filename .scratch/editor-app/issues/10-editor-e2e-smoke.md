# 10 — Editor E2E smoke tests

Status: resolved
Blocked by: editor-app/04, editor-app/05, editor-app/06, editor-app/07

Playwright E2E: fresh profile → sample doc renders (page count >1); edit text → page count changes after debounce; change preset + page size → canvas updates; import/export .md round-trip; library create/rename/delete; Client Export → assert hidden iframe HTML structure (print dialog itself can't be automated); dark toggle persists; locks visible on gated controls. Visual regression screenshots of Paper Canvas (light+dark) at 2 viewport sizes.

**Accepts**: suite green in CI; catches a broken render pipeline or a pane-layout regression.

## Comments

Implemented 2026-09-11 (commits 2da3c9b, 29deb4b, 3c88133).

- **Runner & target**: `@playwright/test` in apps/web (`apps/web/playwright.config.ts`), driving the production bundle — the webServer command builds core + web and serves `vite preview` on :4173, so the suite is self-contained (`pnpm --filter @perfectmarkd/web test:e2e`). One browser context per test = the ticket's "fresh profile" for free. Files in `apps/web/e2e/`, one spec per ticket clause.
- **Coverage map**: onboarding.spec (sample seeds, paginates >1 page, labels "Page N of M", reload reopens without re-seeding) · editor-canvas.spec (typing re-paginates after the 400ms debounce via `///` Page Break markers; Style-preset change re-styles the mounted shadow-DOM page box; page size A4→Letter re-lays out frame width 794→816px) · library.spec (create/rename/delete + Undo; .md import→export byte-identical round-trip via filechooser/download) · export.spec (hidden print iframe captured at insertion via a MutationObserver init-script — the frame is removed right after print(), so reading it later races; asserts sentinel/@page/page boxes/content root/inlined KaTeX/title/footer text and that export page count equals the Paper Canvas count — ADR-0003's single-pipeline guarantee as a runtime assertion; print() stubbed, hint shows once per session) · appearance.spec (dark toggle re-themes chrome, pages stay paper-white, persists across reload) · gates.spec (all five lock families visible across the three tabs; lock opens the pricing modal) · visual.spec (Paper Canvas screenshots, light+dark × 1280×800/1920×1080, baselines committed with Playwright's `-linux` platform suffix; plus structural pane-contract assertions: editor ≈ 38%, inspector 320px, both separators present).
- **Acceptance "catches a broken render pipeline or a pane-layout regression" verified by sabotage** (golden-suite protocol): early-return in the canvas render → 11/12 functional tests fail; `editorDefaultRatio` 0.38→0.55 → the 4 screenshots + the structural pane test fail. Reverted.
- **A real bug fell out of the first run** (the suite working as intended): `deleteDocument` cancelled the pending autosave and read the stored record, so rename→delete inside the 500ms autosave window built the toast/undo snapshot from the pre-edit record. Fixed in 2da3c9b by flushing first — same persist-first contract `exportDocument` already documents. Not asked for by this ticket (scope note per review); the alternative was asserting the broken behavior.
- **Two-axis review fixes applied** (3c88133): CONTEXT.md vocabulary in comments (Page Break marker, Paper Canvas); `PRINT_STUB` reduced to a print()-call counter (the iframe capture is the single document recorder, so the two writers can't disagree); Library drawer locator shared via helpers; redundant post-poll assertion dropped. Deferred: `flushIfActive(id)` extraction if a third flush-before-read appears in store.ts.
- **Notes for humans**: visual baselines are per-platform (regenerate after intentional visual changes with `pnpm --filter @perfectmarkd/web test:e2e:update`); CI green for the new step confirms on push (setup mirrors the golden suite: same pinned `@playwright/test`, same Chromium install step). Pagination via text flow (font metrics spilling a page) is deliberately not asserted here — engine-port/08's goldens pin exact pagination; the smoke uses Page Break markers so it can't flake on metrics. Pre-existing, unrelated: root `pnpm test` passes `--no-webstorage` in NODE_OPTIONS, which Node 22 (this machine) rejects — CI's Node 24 is fine; run `vitest run` directly locally.
