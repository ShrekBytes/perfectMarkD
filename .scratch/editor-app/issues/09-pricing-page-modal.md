# 09 — /pricing page + pricing modal (Phase-1 inert state)

Status: resolved
Blocked by: editor-app/01

`/pricing` route (static content in the SPA): three-column plan comparison from PLAN.md §1 table, AGPL note, "payments launching soon" state in Phase 1. Pricing modal (opened from Inspector 🔒 and from Export dropdown's Server Export item): same comparison, compact; CTA in Phase 1 = "Get notified / coming soon" (mailto or nothing — no email infra). Built so Phase 2 swaps the CTA to the real upgrade flow (billing workstream) without redesign. GitHub + license badge links in footer.

**Accepts**: page + modal render from a single shared plan-data module (single source of truth for plan features/prices, later fed by admin settings).

## Comments

**Implementation** (2026-09-07):

- `apps/web/src/pricing/plans.ts` — the single source of truth: `PLANS` (ids free/pro/premium, USDT prices 0/3/7, blurbs), `FEATURE_ROWS` (the PLAN.md §1 matrix verbatim — `true`/`false` render as included/not-included marks, strings pass through as quotas/"never"), `formatPrice`, `DURATION_NOTE` (durations, 10× annual, manual crypto, nothing auto-renews), `BILLING_LIVE = false`, and the shared `COMING_SOON_NOTE`. Phase 2's admin-settings feed replaces these constants with the same shapes.
- `apps/web/src/pricing/PlanComparison.tsx` — the three-column comparison both surfaces render (acceptance criterion): a semantic `<table>` (columnheaders/rowheaders, sr-only "Included"/"Not included" text) driven entirely by the plans module. `compact` (modal) keeps every row, drops blurbs, tightens type. The per-plan CTA is one component: free = working "Open the editor"; paid = inert "Coming soon" whose disabled state, style, and label all read `BILLING_LIVE` — Phase 2 flips the flag and wires the click to the Order flow without touching the layout.
- `apps/web/src/pricing/PricingModal.tsx` — self-contained dialog (`onClose` only), so editor-app/05's Inspector locks can mount it as-is. Compact comparison + coming-soon note; Escape, backdrop, and Close button all close; "Open the editor" closes it (the editor is already underneath).
- `apps/web/src/pricing/PricingPage.tsx` — the /pricing page: header (wordmark + "Open the editor" + theme toggle), hero, comparison card, coming-soon line, "Good to know" bullets (free-forever styling list from PLAN.md §1, duration terms, AGPL note), footer with AGPL-3.0 badge + GitHub link + plugin credit (PLAN.md §6: "the web app links to it").
- `apps/web/src/router.tsx` — minimal History-API routing (~70 lines, no react-router for two static surfaces): `useRoute`/`navigate`/`Link` (respects modified clicks), `routeForPath('/pricing')`, everything else → editor until the server's hidden `/export` route lands (ADR-0003). Deep links to /pricing need the host's SPA fallback — Vite dev has one; **server/06 must serve index.html for /pricing**.
- `App.tsx` — route switch; `export/ExportSplitButton.tsx` — dropdown gains the inert "Server Export · soon" item that opens the modal (the Inspector path arrives with editor-app/05); `shell/icons.tsx` — CheckIcon, ServerIcon.
- Follow-ups for a human: the footer's `GITHUB_URL`/`LICENSE_URL` are placeholders (`https://github.com/ShrekBytes/perfectMarkD`) pending the org/repo decision (PLAN.md §7 item 3) — one constant pair to flip in `PricingPage.tsx`. The spec's "Get notified" mailto variant was dropped in favor of the issue's "or nothing" branch (no email infra, no address exists).
- Tests: workspace suite 487 → 516 (+29). New: `plans.test.ts` (tier table invariants incl. quota cells, gated-feature marks, premium-only rows, inert flag), `router.test.tsx` (path mapping, pushState/popstate, Link modified-click passthrough), `PlanComparison.test.tsx`, `PricingModal.test.tsx` (close paths), `PricingPage.test.tsx` (footer hrefs, AGPL note), `App.test.tsx` (editor at /, pricing at /pricing + wordmark swap), and a Server Export menu-item case in `ExportSplitButton.test.tsx`.

**Review outcomes (two-axis)**: deduped the coming-soon copy into `plans.ts`; made `BILLING_LIVE` actually drive the paid CTA (it previously gated only the notes — flipping it would have hidden the notes while CTAs still read "Coming soon"); dropped a test-only `data-compact` hook; reused the computed text-size class. Spec-axis: PLAN.md §1 table verified cell-by-cell; the Inspector 🔒 trigger is intentionally unwired (Inspector is editor-app/05); modal confirmed reusable for it.

**Acceptance, verified in a real browser (Chromium via dev server):** deep-linked `/pricing` renders the full matrix (dark + light, toggle works); wordmark swaps back to the editor via history push; Export ▾ → "Server Export · soon" opens the compact modal over the editor; Escape and the free CTA both close it; footer badge/links present. Screenshots confirmed layout in both themes. Full workspace suite 516/516, typecheck/eslint/prettier clean, `vite build` passes. (One unrelated flake observed once in `AppShell.test.tsx` first-run seeding under suite parallelism; green on repeat — pre-existing timing margin, unchanged by this ticket.)
