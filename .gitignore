node_modules/
dist/
coverage/
*.tsbuildinfo

# Environment
.env
.env.*
!.env.example

# Local SQLite data (default DB_PATH)
data/
*.db
*.db-wal
*.db-shm

# OS / editor noise
.DS_Store
*.log

# Command-runner scratch files (one empty file per shell command, repo root)
_tmp_*

# Playwright artifacts (web E2E; baselines under apps/web/e2e/*.snapshots are committed)
apps/web/test-results/
apps/web/playwright-report/
